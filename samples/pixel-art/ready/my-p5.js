// 変数の宣言

function setup() {
  // フレームレートを24フレーム/秒に設定
  // ＰＣがうなるようであればここを調整
  // frameRate(24);

  // キャンバスの大きさを720x400ピクセルに指定
  createCanvas(720, 400);
  // 背景色をカラーコード#E0F4FFに指定
  background("#E0F4FF");
}

function draw() {}

function init() {}
